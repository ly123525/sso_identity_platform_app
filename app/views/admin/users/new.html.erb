<div class="d-flex align-items-center justify-content-between mb-4">
  <div>
    <h1 class="h3 mb-1"><%= t('admin.users.new.title') %></h1>
    <p class="text-muted mb-0"><%= t('admin.users.new.subtitle') %></p>
  </div>
  <%= link_to t('admin.users.actions.back'), admin_users_path, class: 'btn btn-outline-secondary' %>
</div>

<%= render 'form', user: @user, submit_label: t('admin.users.actions.create'), show_password_fields: true %>
